{{-- @extends('app')
@section('title', 'Categories')
@section('content')
    <h1>Categories</h1>

    <x-card class="bg-indigo-200">
        @if (count($categories) > 0)
            @foreach ($categories as $category)
                <x-category-list :ctg="$category" />
            @endforeach
        @endif

    </x-card>
@endsection --}}
